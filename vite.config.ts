import { defineConfig } from 'vite';

export default defineConfig({
  // Change to '/' if the repository is renamed to suchitbasineni.github.io or a custom domain is added.
  base: '/portfolio/',
});
